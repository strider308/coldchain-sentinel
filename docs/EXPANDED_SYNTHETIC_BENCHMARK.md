# Expanded Synthetic Benchmark Refresh

Phase 21B ingests `artifacts/expanded_synthetic_benchmark_phase21.json` as a validated, read-only evidence summary.

Routes:

- `/expanded-benchmark` and `/expanded-benchmark.json`
- `/benchmark-refresh` and `/benchmark-refresh.json`

The artifact covers fourteen synthetic scenario families, benchmark comparisons, and an offline CPU/GPU training timing summary. GPU/Jupyter was used offline only. The live app remains stdlib-only and requires no GPU, notebook, external service, or autonomous operational action.

Deterministic rules remain authoritative, and all benchmark output remains advisory-only synthetic evidence.
